import 'package:flutter/material.dart';

class DoneTasks extends StatelessWidget {
  const DoneTasks({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child:
      Text('Donee Taskssss',style: TextStyle(
        fontSize: 26,
        fontWeight: FontWeight.bold,
      ),),
    );
  }
}
